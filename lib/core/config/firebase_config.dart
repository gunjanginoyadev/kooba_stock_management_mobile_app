/// Firebase backend configuration.
///
/// Options live in [DefaultFirebaseOptions] (from google-services.json).
/// Set [isConfigured] to false only if you need to run the UI without Firebase.
class FirebaseConfig {
  /// True when Firebase should be initialized and used.
  static const bool isConfigured = true;
}
